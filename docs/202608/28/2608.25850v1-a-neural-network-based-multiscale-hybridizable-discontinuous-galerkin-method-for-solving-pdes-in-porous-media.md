---
title: A Neural-network-based multiscale Hybridizable Discontinuous Galerkin method for solving PDEs in porous media
title_zh: 一种基于神经网络的多尺度杂交间断伽辽金方法，用于求解多孔介质中的偏微分方程
authors: "Tony Haines, Ke Shi"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.25850v1"
tags: ["query:ag"]
score: 9.0
evidence: 神经网络代理加速多尺度有限元求解多孔介质PDE
tldr: 针对多尺度混合间断伽辽金方法中局部算子构建成本高的问题，提出神经网络加速方法。在参考块上训练网络预测离散Dirichlet-to-Neumann算子，替代重复细尺度求解。中对比度渗透率场下，相对误差仅百分之几，在线计算成本降低约5-16倍；高对比度下误差显著，需改进粗空间。展示了神经代理的加速潜力，并指出误差放大问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.25850v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 668, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25850v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 812, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25850v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1604, \"height\": 905, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.25850v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1868, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 484, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1306, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1465, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1171, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1556, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1335, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25850v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1410, \"height\": 233, \"label\": \"Table\"}]"
motivation: 标准多尺度HDG方法需反复进行细尺度局部求解来构建算子，在线成本高，尤其系数复杂时计算负担大。
method: 在参考块上训练神经网络，以系数场为输入，预测离散Dirichlet-to-Neumann算子，替代MsHDG中的局部细尺度求解。
result: 中等对比度下相对误差百分之几，总在线成本降低5-16倍；高对比度下标准粗空间本身误差大，神经算子误差随富化加剧。
conclusion: 神经替代可加速多尺度HDG计算，但需发展更有效的粗空间，并深入理解高对比度问题的误差放大机制。
---

## 摘要
我们开发了一种神经网络加速的多尺度杂交间断伽辽金方法，用于求解具有非均匀系数的椭圆问题。该方法保留了标准MsHDG的局部到全局结构：粗块上的细尺度HDG问题定义了离散的Dirichlet-to-Neumann算子，这些算子通过标准MsHDG全局骨架方程进行组装。为了降低构建这些局部算子的成本，我们在参考块上定义的系数场上训练神经网络，并使用预测的算子代替重复的细尺度局部求解。数值实验评估了所得NN-MsHDG方法的准确性和在线效率。对于具有中等对比度的二维二值渗透率场，神经方法以百分之几的相对建模误差再现了标准MsHDG解，同时将总在线计算成本降低为原来的约1/5至1/16，具体取决于粗迹维数。在高对比度区域，所选择的多项式粗迹空间在标准MsHDG方法中已经产生了显著的离散化误差，这表明需要更有效的粗空间，例如系数自适应的谱迹空间。此外，学习得到的局部算子会引入建模误差，且随着迹空间的加密，这些误差变得严重。这些结果展示了神经代理模型在加速多尺度HDG计算方面的潜力，同时也强调了改进算子表示以及更好地理解高对比度问题中误差放大的必要性。

## Abstract
We develop a neural-network-accelerated multiscale hybridizable discontinuous Galerkin method for elliptic problems with heterogeneous coefficients. The method preserves the standard MsHDG local-to-global structure: fine-scale HDG problems on coarse blocks define discrete Dirichlet-to-Neumann operators, which are assembled through the standard MsHDG global skeleton equations. To reduce the cost of constructing these local operators, we train a neural network on coefficient fields defined on a reference block and use the predicted operators in place of repeated fine-scale local solves.   The numerical experiments assess both the accuracy and online efficiency of the resulting NN-MsHDG method. For two-dimensional binary permeability fields with moderate contrast, the neural method reproduces the standard MsHDG solution with relative modeling errors of a few percent while reducing the total online computational cost by factors of approximately 5 to 16, depending on the coarse trace dimension. In the high-contrast regime, the chosen polynomial coarse trace spaces already yield substantial discretization errors in the standard MsHDG method, indicating the need for more effective coarse spaces, such as coefficient-adapted spectral trace spaces. In addition, the learned local operators introduce modeling errors that become severe as the trace space is enriched. These results demonstrate the potential of neural surrogates for accelerating multiscale HDG computations while also highlighting the need for improved operator representations and a better understanding of error amplification in high-contrast problems.

---

## 论文详细总结（自动生成）

## 1. 核心问题与研究动机

- 论文关注的是**多孔介质中具有强非均匀系数的椭圆问题**，例如 Darcy 流动，其控制方程为  
  `-∇·(κ(x)∇u)=f`。
- 经典有限元/HDG 方法需要细网格分辨介质异质性，导致系统规模巨大；在参数依赖、时间依赖或非线性问题中，相关求解还要重复多次。
- 标准多尺度 HDG（MsHDG）方法将全局未知量压缩到粗骨架上的迹变量，但构造每个粗块的**离散 Dirichlet-to-Neumann（DtN）算子**仍需要对细尺度局部 HDG 矩阵求逆，这是主要计算瓶颈。
- 本文的核心动机是：**用神经网络代理模型替代粗块局部 DtN 算子的重复构建**，从而在保留 MsHDG 全局耦合结构的前提下降低在线计算成本。

## 2. 方法论：NN-MsHDG

### 核心思想

- 保持标准 MsHDG 的“局部-全局”结构：
  - 每个粗块上求解细尺度 HDG 局部问题，得到 DtN 矩阵 `S_i(α)` 和源项贡献向量 `g_i(α,f)`；
  - 将这些局部算子按标准全局骨架方程组装，得到粗迹未知量的全局系统。
- 神经网络只替代局部算子的计算：  
  `κ|Ωi ↦ (S_i(κ^{-1}), g_i(κ^{-1}, 1))`，全局组装、迹求解和局部重构保持不变。

### 关键技术细节

- **参考块设置**：训练在参考块 `K=[0,1]^2` 上进行；二维实验中系数为 8×8 二值渗透率图像（低/高渗透率两相），细网格为 32×32 均匀方格并二分为三角形。
- **粗迹空间**：每条粗边按 trace level `n=0,1,2` 均匀分成 `2^n` 段，每段采用不连续分片线性函数，粗迹维数分别为 8、16、32。
- **神经网络输入输出**：
  - 输入：归一化的 `log10(κ)` 图像，维度恒定为 64；
  - 输出：对称矩阵 `S(α)` 的上三角元素和向量 `g(α,1)`，输出维度随 `n` 增大。
- **网络结构**：3 层卷积（3×3 卷积、GELU 激活）+ 2 层全连接 + 线性输出层。
- **损失函数**：在归一化数据损失之外，加入：
  - 局部算子作用损失 `L_act`；
  - 能量一致性损失 `L_en`；
  - 常数迹零空间惩罚 `L_null`；
  - 这些辅助损失使预测的 DtN 矩阵在全局组装相关的迹方向上更准确。

### 算法流程

1. 对参考块采样大量随机二值渗透率分布；
2. 用细尺度 HDG 局部求解器计算每个样本的 `(S,g)` 作为标签；
3. 训练神经网络学习“系数图像 → 局部算子”的映射；
4. 在线阶段：对每个物理粗块，用网络预测 `(S,g)`；
5. 按标准 MsHDG 公式组装全局骨架系统并求解粗迹；
6. 通过局部求解重构细尺度解。

## 3. 实验设计

### 数据集与场景

- **一维验证**：粗区间上的均匀渗透率单元，系数从二值集合 `{1,10}` 或 `{1,10^4}` 中独立采样；学习目标是区间等效电导（调和平均），与解析 DtN 形式对比。
- **二维低对比度**：`κ ∈ {1,10}`；域 `[0,1]^2` 分成 5×5 粗块；细网格 `h=1/160`；迹层级 `n=0,1,2`。
- **二维高对比度**：`κ ∈ {1,10^4}`；相同粗分、细网格、迹空间设置。
- **训练数据**：每个对比度生成 5000 个参考块系数样本，80% 训练、20% 验证；全局测试使用 20 个独立保留的系数实现。

### 对比方法

- 细尺度 HDG 参考解 `u_h^REF`；
- 标准 MsHDG 解 `u_H^MS`（局部算子由细尺度 HDG 求解器精确计算）；
- NN-MsHDG 解 `u_H^NN`（局部算子由神经网络预测）。

### 误差指标

- `E_MS`：标准 MsHDG 相对细网格参考解的误差；
- `E_NN`：NN-MsHDG 相对细网格参考解的误差；
- `E_model`：NN-MsHDG 与标准 MsHDG 之间的建模误差。
- 另外，高对比度实验还报告局部 DtN 在光滑迹和随机迹上的相对作用误差。

## 4. 资源与算力

- 文中给出了计算环境：
  - CPU：Intel Core i7-14700F；
  - 内存：32 GB；
  - GPU：NVIDIA GeForce RTX 4060 8 GB；
  - 软件：MATLAB R2026a 进行 HDG/MsHDG；Python 3.11.7 + PyTorch 2.2.2 进行神经网络训练和推理；
  - MATLAB 为双精度，神经网络为单精度。
- 训练细节：AdamW 优化器、初始学习率 3×10⁻⁴、batch size 128、随机种子 123；trace level `n=0,1,2` 分别训练 600、900、1200 epochs。
- **没有明确报告每次训练的具体 GPU 耗时、数据生成耗时或 GPU 利用率**；论文只说明计时结果不包括离线数据生成和训练时间。

## 5. 实验数量与充分性

### 实验数量

- 一维：2 种对比度 × 3 种系数分辨率（Nκ=16,32,64）× 4 种粗分（Nc=5,10,20,40），每种情况在 25 个全局实现上取平均，规模较充分。
- 二维：2 种对比度 × 3 种迹层级，每种情况在 20 个全局实现上评估；训练与验证另有 5000 个局部样本。
- 计时：15 次重复测试。
- 总体上，**作为概念验证足够**，能够展示方法在中低对比度下的准确性和加速比，以及高对比度下的困难。

### 充分性与公平性

- 优点：标准 MsHDG 与 NN-MsHDG 使用相同的细/粗离散、相同稳定化参数和相同全局耦合方式，仅替换局部算子，因此 `E_model` 能公平度量神经网络引入的额外误差。
- 不足：缺乏消融实验（如不同损失项的贡献、网络深度/宽度的影响、训练集大小敏感性）；没有使用多个训练随机种子进行稳定性验证；没有真实地质渗透率数据，只有合成的伯努利二值随机场；没有测试非矩形几何、不同粗块形状或源项变化；也没有推广到时间依赖/非线性问题。

## 6. 主要结论与发现

- **一维：神经网络学习的局部 DtN 算子可以准确装配**；建模误差通常低于 1%，误差不会随粗块数量增加而累积。
- **二维低对比度**：
  - NN-MsHDG 与标准 MsHDG 非常接近：`E_model` 在 `n=0,1` 约为 1.3%，`n=2` 约为 3.0%；
  - 标准 MsHDG 误差随迹空间加密从 9.8% 降到 3.5%；
  - 在线装配时间几乎不随迹维数增加，而标准 MsHDG 装配时间显著增长；
  - 在线总加速比从约 4.8 倍提高到 16.3 倍。
- **二维高对比度**：
  - 多项式分片线性粗迹空间本身无法有效逼近细尺度解（标准 MsHDG 误差在 `n=2` 仍约 43%）；
  - 神经网络预测的局部 DtN 在局部作用误差上约为 1e-2，但装配到全局后建模误差很大（`E_model` 从 0.28 增至 0.90）；
  - 说明高对比度问题对局部 DtN 误差非常敏感，存在明显的误差放大效应；
  - 需要系数自适应的谱迹空间（如 spectral MsHDG）以及更稳健的算子表示。
- **总体结论**：神经网络代理能够显著加速多尺度 HDG 计算，但当前方法适用于中低对比度及合适粗空间的情形；高对比度下仍需解决粗空间构造和误差放大问题。

## 7. 优点

- **保留经典数值方法结构**：不是端到端替代 PDE 求解器，而是只在局部算子构建环节引入神经网络，可解释性强，易于嵌入现有 MsHDG 代码。
- **输出维数紧凑**：利用 DtN 矩阵对称性，只预测上三角条目和源向量，而不是完整逆矩阵，降低学习难度。
- **物理启发的损失函数**：在数据损失之外加入作用损失、能量损失和常数迹零空间惩罚，使预测算子更接近全局求解所需的行为。
- **误差分离清晰**：通过 `E_MS`、`E_NN`、`E_model` 严格区分离散化误差和神经网络建模误差，分析较为严谨。
- **一维解析验证**：利用一维 DtN 算子的解析形式验证学习和组装流程，增强了可信度。
- **计时测试较规范**：固定工作站、关闭并行池、多次重复并排除离线成本，给出了较为一致的加速比结论。

## 8. 不足与局限

- **高对比度下失效风险高**：即使局部 DtN 作用误差只有 1e-2，全局建模误差也可能达到 90%，缺乏理论分析解释误差放大机制。
- **粗迹空间选择受限**：仅用不连续分片线性迹空间，对高对比度问题不合适；论文未与 spectral MsHDG 或系数自适应粗空间结合。
- **在线计时未包含离线成本**：训练和数据生成开销被排除，对实际部署的总成本估计不够完整。
- **没有报告训练时间的具体数值**，无法评估离线成本是否可接受。
- **实验场景较窄**：仅二维二值渗透率、固定源项 f≡1、固定几何参考块；未涉及三维、连续变化系数、非线性/时间依赖问题。
- **局部误差度量与全局误差不匹配**：光滑/随机迹上的局部作用误差不能保证全局骨架系统的稳定性，说明现有损失/诊断指标可能不够充分。
- **单精度/双精度差异**：神经网络使用单精度，MATLAB 精确求解使用双精度，但论文未讨论精度差对误差的定量影响。
- **未提供可用代码或预训练模型**，可复现性受限。

（完）
