---
title: "ENPINN: Energy-Norm-Guided Gradient-Enhanced PINNs for Generalized Transport Problems with Sharp Gradients"
title_zh: ENPINN：面向具有陡峭梯度的广义输运问题的能量范数引导梯度增强PINN
authors: "Subhendu Maity, Pratibhamoy Das, Arihant Patawari, Ameya D. Jagtap"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29499v1"
tags: ["query:ag"]
score: 8.0
evidence: 能量范数梯度增强的PINN方法面向尖锐梯度输运问题，可直接迁移至地下水流与溶质运移模拟
tldr: 物理信息神经网络求解含陡峭梯度的广义输运问题时，边界层和内部层分辨率不足。本文提出能量范数增强的ENPINN，将梯度信息与变分结构融入损失函数。通过分析弱损失PINN和梯度增强PINN的局限，系统构造了ENPINN，并理论上证明其能量误差与损失的关系及近似存在性。数值实验表明，ENPINN在多种含陡峭梯度的对流扩散反应问题中优于现有变体。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29499v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1552, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1689, \"height\": 610, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1587, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1698, \"height\": 561, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1556, \"height\": 675, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 671, \"height\": 618, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1567, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29499v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 674, \"height\": 570, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29499v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1525, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29499v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1525, \"height\": 426, \"label\": \"Table\"}]"
motivation: 现有PINN难以捕捉边界层和内部层的陡峭梯度，导致层主导解误差较大。
method: 在损失函数中引入能量范数引导的残差导数项，结合变分结构与梯度增强，构建ENPINN。
result: 在燃烧模型、多尺度系统、二维Burgers方程及三维时间依赖问题中，ENPINN精度显著优于WLPINN和gPINN。
conclusion: 能量范数增强可有效提升PINN对陡峭梯度解的分辨能力，并具备理论保证。
---

## 摘要
物理信息神经网络（PINN）已成为求解偏微分方程（PDE）的传统数值方法的一种无网格替代方案。然而，它们在捕捉陡峭梯度方面的能力有限，在解析边界层和内部层时可能导致显著误差。在此，我们引入一种能量范数增强的PINN（ENPINN），将梯度信息和变分结构融入损失函数中，以提高层主导解的分辨率。我们首先研究两种相关公式：弱损失PINN（WLPINN），它将测试函数纳入传统PINN残差中；以及梯度增强PINN（gPINN），它通过PDE残差的空间导数来增强损失。通过分析这些公式，我们识别出它们在解析陡峭解梯度方面的局限性，并系统性地构建ENPINN提供了动机。我们从理论上确立了能量范数误差如何依赖于ENPINN损失，并表明适当修改的残差导数项对于准确捕捉边界层至关重要。我们进一步证明了具有任意小能量误差的神经网络逼近的存在性，并推导了相应的导数界，为所提出的框架提供了理论基础。通过系统性地与现有PINN变体在表现出陡峭梯度的对流-扩散-反应问题上的比较，评估了ENPINN的性能。数值实验包括一个燃烧模型、一个耦合多尺度系统、一个具有内部层的二维Burgers方程，以及一个三维时间依赖问题。

## Abstract
Physics-informed neural networks (PINNs) have emerged as a meshless alternative to conventional numerical methods for solving partial differential equations (PDEs). However, their limited ability to capture sharp gradients can lead to substantial errors when resolving boundary and interior layers. Here, we introduce an energy-norm-enhanced PINN (ENPINN) that incorporates gradient information and variational structure into the loss function to improve the resolution of layer-dominated solutions. We first examine two related formulations: weak-loss PINNs (WLPINNs), which incorporate test functions into the conventional PINN residual, and gradient-enhanced PINNs (gPINNs), which augment the loss with spatial derivatives of the PDE residual. By analyzing these formulations, we identify their limitations in resolving steep solution gradients and motivate the systematic construction of ENPINN. We establish theoretically how the energy-norm error depends on the ENPINN loss and show that a suitably modified residual-derivative term is essential for accurately capturing boundary layers. We further establish the existence of neural-network approximations with arbitrarily small energy error and derive corresponding derivative bounds, providing a theoretical foundation for the proposed framework. The performance of ENPINN is assessed through systematic comparisons with existing PINN variants for convection-diffusion-reaction problems exhibiting steep gradients. Numerical experiments include a combustion model, a coupled multi-scale system, a two-dimensional Burgers equation with an interior layer, and a three-dimensional time-dependent problem.

---

## 论文详细总结（自动生成）

## 论文总结：ENPINN：面向具有陡峭梯度的广义输运问题的能量范数引导梯度增强PINN

### 1. 核心问题与整体含义

- **研究背景**：物理信息神经网络（PINN）已成为求解偏微分方程（PDE）的无网格替代方案，广泛应用于高速流、燃烧、多相流、磁流体动力学等科学计算领域。然而，PINN在求解含陡峭梯度的广义对流-扩散-反应输运问题时存在显著挑战——标准的L2残差最小化存在"谱偏差"，即使L2误差很小，也无法保证对边界层和内部层大梯度的准确解析。
- **问题核心**：在燃烧理论、传热传质等应用中，由小扰动参数（ε, μ）引起的边界层/内部层现象使解呈现陡峭梯度，给PINN类方法带来严重拟合困难。现有方法（如VS-PINN变量缩放、PAPINN参数渐近）大多针对单一小参数问题，而广义输运问题往往涉及多个扰动机制的耦合（对流主导 vs 反应主导），更具挑战性。
- **整体含义**：本文通过将**能量范数**引入损失函数构建（结合变分测试函数与梯度信息），提出ENPINN框架，旨在理论上和实验上同时突破PINN对陡峭梯度分辨率不足的瓶颈，为层主导问题的神经PDE求解器提供可靠方案。

### 2. 方法论：ENPINN框架

- **核心思想**：将能量范数信息（H1范数与ε加权）嵌入弱形式的损失函数，即在传统PINN残差基础上，同时引入**测试函数加权**和**残差导数项**，双管齐下增强对边界层的"感知"。
- **关键技术细节**：
  - 引入边界拟合测试函数 \(V(x,y) \in H^1_0(\Omega)\)，在边界附近取极小值，以此缩放边界层区域的残差（类比流场稳定化参数思想）。
  - 定义**修正残差** \(N_{int,p} = N_{res,p} \times V(x,y)\)。
  - 定义**导数修正残差**（关键创新点）：
    \(N_{(int,p)x} = N_{res,x} V + N_{res} V_x\)，\(N_{(int,p)y} = N_{res,y} V + N_{res} V_y\)。
  - 损失函数（连续形式，式14）：
    \[
    \mathcal{L}^{en}_C = \lambda_{int}\|N_{int,p}\|^2 + \lambda_{int}\|N_{(int,p)x}\|^2 + \lambda_{int}\|N_{(int,p)y}\|^2 + \lambda_{spb}\|N_{spb,p}\|^2 + \lambda_{ini}\|N_{ini,p}\|^2
    \]
  - 内部残差与测试函数V的**乘积的导数**（而非单纯残差的导数）是ENPINN区别于gPINN的本质。
- **理论分析体系（三大支柱）**：
  - **存在性**（Theorem 4.1）：针对多参数BVP（式18），证明对任意误差界B，存在网络参数 \(\hat{p}\) 使ENPINN损失小于B（基于Tanh网络逼近指数函数与恒等函数的构造性证明）。
  - **导数界**（Theorem 4.3）：证明ENPINN逼近解的导数界与精确解的尺度一致（如 \(|\partial_x^l u^{L}_{N,\hat{p}}| \le C\varepsilon^{-l_x/2} e^{-\sqrt{b/\varepsilon}x}\)），从机制上保证了网络能复现边界层的梯度量级。
  - **误差估计**（Theorem 5.1 & 5.5）：在 \(\mu^2 \ge \zeta\varepsilon/\alpha_0\) 和 \(\mu^2 \le \zeta\varepsilon/\alpha_0\) 两种参数状态下，分别导出L2范数和能量范数的推广误差界，证明能量误差由训练误差、求积误差和L2推广误差共同控制。
  - **系统扩展**（Theorem 5.12 & 5.13）：将理论分析推广至耦合多尺度系统。

### 3. 实验设计

- **测试场景（5组）**：
  1. **Example 6.1**：含解析解的2D时间依赖对流-扩散-反应模型（ε=1e-3, μ=1e-4），验证收敛性。
  2. **Example 6.2**：非线性燃烧流动模型（热爆炸，源项 \(f=\sigma e^{\gamma u}\)），参考解由SciPy BDF求解器产生。
  3. **Example 6.3**：3D双参数PDE（ε=1e-5, μ=1e-2），属于 \(\mu^2 \ge \varepsilon\) 状态，gPINN表现恶化场景。
  4. **Example 6.4**：2D Burgers方程（ε=0.01/π），考核内部层捕捉。
  5. **Example 6.5**：2D耦合多尺度系统（对角扩散系数diag(1e-4, 1e-2)），验证对耦合系统的扩展性。
- **对比方法**：VSPINN、WLPINN（仅含测试函数加权）、gPINN（仅含残差导数）、ENPINN（两者兼具）。
- **评估指标**：L2误差（\(E^2_{L2}\)）与能量误差（\(E^{en}_G = E_{L2} + \varepsilon\|\nabla e\|_{L2}\)），并有分区加密研究（Nx=Ny=5/15/25, Nt=5/10/15）。
- **激活函数与优化器**：dual-nested Tanh激活函数；Adam + L-BFGS两阶段训练。

### 4. 资源与算力

- **论文中未明确说明GPU型号、数量、总训练时长或参数量**等具体硬件配置。
- 仅报道了各实验的训练轮数配置：Example 6.1/6.3采用10k Adam轮 + 10k L-BFGS轮；Example 6.2采用5k Adam + 5k L-BFGS；Example 6.5采用15k Adam + 15k L-BFGS。
- 每个实验对多种PINN变体使用相同超参数配置（"over the same hyperparameters"），并采用多随机种子评估方差，但计算成本总量无从考证。

### 5. 实验数量与充分性

- **实验数量**：共5组数值实验，每组实验包含不同网格分区（3种）、3-4种方法对比（VSPINN/WLPINN/gPINN/ENPINN）以及多随机种子重复。
- **充分性评价**：
  - **优点**：覆盖面较广——从标量到耦合系统、从2D到3D、从线性到非线性、从解析解到数值参考解均有涉及；同时包含收敛性验证（分区加密）和参数状态切换（\(\mu^2 \ge \varepsilon\) 与 \(\mu^2 \le \varepsilon\)），较充分地验证了理论声明的适用范围。
  - **不足**：缺少针对不同测试函数V的选取消融实验，也缺少λ系数敏感性与网络结构（层数/宽度）影响的系统性研究。对燃烧模型（Example 6.2）未报告与其他方法的对比误差表（只有ENPINN单一方法），对比公平性略显不足。

### 6. 主要结论与发现

- **ENPINN显著优于gPINN与WLPINN**：在能量范数误差方面一致领先；在多数场景下L2误差也最优。Example 6.3中虽WLPINN的L2误差前期略优，但ENPINN在L-BFGS后期持续下降，且能量误差更优。
- **理论完备性**：证明了ENPINN逼近的存在性、导数界与能量范数误差上界，且数值误差随网格加密单调下降（表1、表2），与理论吻合。
- **机制分析**：WLPINN通过测试函数缩放边界残差，能改善边界条件施加，但缺乏对解导数的直接约束；gPINN虽加入残差导数，但其残差与导数随ε、μ的逆幂增长，在边界层内损失放大。ENPINN借助测试函数V的乘积结构抑制了这一逆幂增长，从而兼具两者的优点。
- **通用性**：方法无需知晓层位置与宽度的先验知识，可自然扩展至耦合系统，在燃烧模型、Burgers方程内部层、3D问题中均有效。

### 7. 优点

- **理论深度高**：从存在性→稳定性→收敛性构建了完整的理论链条，避免了多数PINN变体仅靠实验说明的局面。
- **机制设计巧妙**：测试函数与残差导数的"乘积结构"在数学上消弭了gPINN的逆幂项放大问题，与理论分析自洽。
- **应用范围广**：覆盖多参数状态、高维（3D）、非线性（燃烧、Burgers）与耦合系统，且无需层位置先验知识，实用性强。
- **对比实验规范**：使用多随机种子报告均值±方差，并统一超参数，透明且可复现。

### 8. 不足与局限

- **计算成本**：引入残差的二阶空间导数项，使ENPINN计算代价与gPINN相近，比标准PINN高；论文自认需要更有结构性的测试函数来降本增效。
- **测试函数V的选择缺乏指导性原则**：文中仅说明V应在边界附近取小值，但未给出一套系统化的构造规则，实际应用中可能依赖经验试错。
- **场景限制**：主要处理静态/固定边界层结构，对于移动层、激波传播等动态层结构问题尚需框架扩展；理论分析主要基于线性化形式的输运方程，对强非线性问题的适用性论证有限。
- **实验资料部分缺失**：未提供具体算力列表，燃烧模型未做完整对比（缺乏与gPINN/WLPINN的数值对比），影响对公平性的全面判断。
- **数据可用性声明**为"Data sharing is not applicable"，无代码、无数据集链接，复现门槛较高。

（完）
